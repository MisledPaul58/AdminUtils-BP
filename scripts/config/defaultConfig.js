/**
 * This is the config that will be loaded by default *only* when loading the world for the first time or when a factory reset is done.
 */
export const defaultConfig = {
    thanksMessage: true,
    adminTag: "-auadmin",
    ownerTag: "owner"
};
