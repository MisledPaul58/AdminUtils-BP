/**
 * This is the config that will be loaded by default when loading the world for the first time.
 */
export const defaultConfig = {
    thanksMessage: true,
    adminTag: "-auadmin",
    ownerTag: "owner"
};
